<!DOCTYPE html>
<html lang="en">
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
</head>
<body>
    <p> Hi <%= @user.first_name %>,</p>
    <p>
      Thank you for your order!
    </p>
    <h3>
      Order# <%= @order.id%>
    </h3>
    <p>
      <%  @order.line_items.each do |item| %>
        <li>
          <%= item.quantity %>x 
          <%= item.product.name %>
          <%= humanized_money_with_symbol (item.product.price * item.quantity) %>
        </li>
      <% end %>
    </p>
    <p>
      Your Total is: <%= humanized_money_with_symbol @order.total%>
    </p>
    <p>
      We'll send another email when it's shipping out! Thank you for shopping with us!
    </p>
</body>
</html>